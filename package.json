{
  "name": "projektinspire-booking",
  "version": "0.1.0",
  "private": true,
  "description": "Projekt Inspire booking frontend connected to the shared Laravel backend.",
  "type": "module",
  "scripts": {
    "start": "node preview.mjs",
    "dev": "node preview.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node scripts/check-browser.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
